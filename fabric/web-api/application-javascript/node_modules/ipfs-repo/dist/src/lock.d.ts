export type LockCloser = import('./types').LockCloser;
/**
 * Check if the repo in the given directory is locked.
 *
 * @param {string} dir
 * @returns {Promise<boolean>}
 */
export function locked(dir: string): Promise<boolean>;
/**
 * Lock the repo in the given dir.
 *
 * @param {string} dir
 * @returns {Promise<LockCloser>}
 */
export function lock(dir: string): Promise<LockCloser>;
//# sourceMappingURL=lock.d.ts.map