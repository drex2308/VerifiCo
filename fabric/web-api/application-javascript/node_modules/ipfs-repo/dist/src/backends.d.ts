export type Datastore = import('interface-datastore').Datastore;
export type Backends = import('./types').Backends;
export type Options = Required<import('./types').Options>;
/**
 * @typedef {import('interface-datastore').Datastore} Datastore
 * @typedef {import('./types').Backends} Backends
 * @typedef {Required<import('./types').Options>} Options
 */
/**
 *
 * @param {Backends} name
 * @param {string} path
 * @param {Options} options
 * @returns {Datastore}
 */
declare function createBackend(name: Backends, path: string, options: Options): Datastore;
export { createBackend as create };
//# sourceMappingURL=backends.d.ts.map